Raw input snapshots are local-only and ignored by Git. Their filenames, sizes and
SHA-256 hashes are recorded in ../processed/source_manifest.json.
Run scripts/ingest_material_data.py --source-dir <input directory> to reproduce.
PackWISE's large image archive is inspected in place, not copied or extracted.
Do not redistribute unreviewed raw datasets or personal local paths in deployment.
